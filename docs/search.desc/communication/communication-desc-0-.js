searchState.loadedDescShard("communication", 0, "Communication Module\nRep3 Party ID\nRep3 Network\nParty 0\nParty 1\nParty 2\nImplements an Error message for when the MPC protocols are …\nAn enum representing the party ID\nReturns the argument unchanged.\nReturns the argument unchanged.\nCalls <code>U::from(self)</code>.\nCalls <code>U::from(self)</code>.\nget next ID\nget previous ID\nBroadcast slices to both parties across multiple networks …\nBroadcast slices to both parties across multiple networks …\nReshare slices across multiple networks in parallel (fast …\nSend-and-receive slices across multiple networks in …\nAn extension trait that adds REP3 specific methods to …\nBroadcast a single <code>F</code> and receive from both parties …\nBroadcast a slice of <code>F</code> and receive from both parties …\nBroadcast a slice of <code>F</code> and receive from both parties (fast …\nReceive a single <code>F</code> via <code>CanonicalDeserialize</code>.\nReceive a <code>Vec&lt;F&gt;</code> via <code>CanonicalDeserialize</code>.\nHigh-performance receive for fixed-size types.\nReceive a single <code>F</code> from next party via <code>CanonicalDeserialize</code>…\nReceive a <code>Vec&lt;F&gt;</code> from next party via <code>CanonicalDeserialize</code>.\nReceive a <code>Vec&lt;F&gt;</code> from next party (fast path for fixed-size …\nReceive a single <code>F</code> from prev party via <code>CanonicalDeserialize</code>…\nReceive a <code>Vec&lt;F&gt;</code> from prev party via <code>CanonicalDeserialize</code>.\nReceive a <code>Vec&lt;F&gt;</code> from prev party (fast path for fixed-size …\nReshare a single <code>F</code>: send to next, receive from prev …\nReshare a slice of <code>F</code> in one round (serialized).\nReshare a slice of <code>F</code> in one round (fast path for …\nSend and receive a single <code>F</code> to/from parties (serialized).\nSend and receive slices of <code>F</code> to/from parties (serialized).\nSend a slice of <code>F</code> – optimized to avoid double-traversal.\nHigh-performance send for fixed-size <code>CanonicalSerialize</code> …\nSend a single <code>F</code> to next party via <code>CanonicalSerialize</code>.\nSend a slice of <code>F</code> to next party via <code>CanonicalSerialize</code>.\nSend a slice of <code>F</code> to next party (fast path for fixed-size …\nSend a single <code>F</code> to prev party via <code>CanonicalSerialize</code>.\nSend a slice of <code>F</code> to prev party via <code>CanonicalSerialize</code>.\nSend a slice of <code>F</code> to prev party (fast path for fixed-size …\nSend a single <code>F</code> directly serialized to the network.")