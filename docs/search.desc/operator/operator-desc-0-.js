searchState.loadedDescShard("operator", 0, "Operator Module\nJoin\naggregation sum for muti-key group by\nAggregation count for muti-key group by. After group_by.\nThis algorithm is to replace ccs23 group_by_count by using …\nUsing valid to count. specifically for distinct count. old …\nAggregation count for muti-key group by. Should after …\naggregation max for muti-key group by. after group_by. …\naggregation min for muti-key group by. after group_by\naggregation sum for muti-key group by. after group_by\naggregation sum for muti-key group by. after group_by\nBefore using this function, the input must be sorted. …\nPerforms the FromLtoR join operation.\nGenerates the permutation for FromLtoR join operation.\nIf valid is 0, key is a big number else keep the key.\n…\nanti-join: return the rows in left table that do not match …\ninner_join primitive for ShareTable\nThe bottleneck of semi-join and left-outer-join. Return …\nSemi-join: keep only the rows in left table that have …\nIn place version Table sort\nTable sort by key using radix sort, with multiple threads, …")